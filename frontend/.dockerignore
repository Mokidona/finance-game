# Контекст сборки фронтенда — исходники и лок, без локально поставленных зависимостей.
# node_modules в контейнере ставится через npm ci; dist пересобирается внутри образа.
node_modules
dist
.DS_Store
*.log
.env
.env.*
npm-debug.log*
.vite
